It is encouraged that all modules should be paired with initiateShopifySession.py.

initiateShopifySession.py should be the primary file where keys and passcodes are retained.

This will decrease the risk of accidentally publishing vulnerable security information in cloned repositories.
